export const COMPUTER_USE_MCP_INSTRUCTIONS =
  'Use the computer_use MCP server to inspect and control native Windows, macOS, or Linux apps on the host desktop. Start with list_apps or list_windows, select a returned window, then call get_window_state with include_text:true and include_screenshot:false for semantic tasks; request a screenshot for visual checks or coordinates; call api only when capability or permission details are needed. list_apps without a query reports apps that currently have targetable windows; pass query to search installed apps, then give a returned id to launch_app. Element ids in a get_window_state tree are directly actionable; when filtering that tree, pass the returned accessibility.snapshotId as find_elements.snapshot_id so it is not rebuilt. A find_elements result returns a top-level snapshotId for reuse. The tree text omits the ubiquitous scroll and context_menu actions from each node\'s actions= list, but invoke_element with action "scroll" or "context_menu" still works on any element id. Prefer invoke_element or set_element_value for accurate semantic actions; use screenshot coordinates only when the app exposes no suitable element. Call computer_use.enable once immediately before the first control action and keep it enabled across the uninterrupted workflow; background actions show a small badge without taking the user\'s focus, mouse, or keyboard. click, drag, scroll, type_text, and press_key use mode:"background" by default and never silently escalate to foreground. Use perform for a short deterministic sequence of element/value/key/text background actions and one final observation; it stops on refusal, error, or unexpected foreground delivery. After a partial batch failure, inspect completed steps and current state before continuing; do not replay the whole batch. Prefer observe:"text" for semantic checks, or "screenshot" or "both" for visual checks. Reuse observation.state instead of immediately requesting the same state again. Results omit what you already sent: an action echoes window only when the window it acted on changed, a perform batch states its window once at the top level instead of per step, and an observation repeats the window only when it changed. Keep using the window you already hold until a result hands you a new one. Read every action\'s delivery or refused object. Use mode:"foreground" only when the user asked for takeover or a refusal explicitly recommends it, and tell the user before taking over; activate_window is a foreground operation; launch_app launches in the background by default and only takes over with mode:"foreground". While the screen is locked, foreground input, activate_window, and foreground launches are refused with screen_locked. On a locked macOS desktop stop and ask the user to unlock the screen instead of retrying: macOS exposes no window content or controls while locked, so screenshots come back unavailable and the accessibility tree is only an app proxy with the menu bar, and background coordinate events are accepted but their effect cannot be observed. A background_unavailable refusal means this action requires explicit foreground mode or a supported semantic action. While a session is enabled and the keep-awake setting is on, the host holds the display awake so the desktop cannot idle-lock mid-session and an enable result reports keepAwake:true; always end a session with computer_use.disable so the display can sleep again. Verify effects with an observation or get_window_state, then call computer_use.disable before pausing for user input, waiting, or finishing. Coordinates are relative to the top-left of the full window frame and must come from the latest screenshot; divide screenshot coordinates by the screenshot\'s reported scale. Refresh stale windows or element snapshots when instructed. Prefer the browser MCP server for web pages. Locked desktops, secure prompts, permission prompts, and password/authentication surfaces require the user.';
