<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 17 (API 37)+ runtime permission for LAN discovery/communication.
         Older platforms ignore the declaration; the app requests it only when
         connecting to a local-network host on API 37+. -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <!-- Requested at the point of use (pairing QR scanner) only. The app remains
         installable and fully usable on camera-less devices: pairing also works by
         pasted link or manual endpoint + token. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Package visibility (Android 11+): without this the camera app is invisible to
         queryIntentActivities, so photo capture always reports itself unavailable. -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".PoracodeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Poracode">

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_poracode" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="poracode_attention_v1" />

        <service
            android:name=".push.PoracodeFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Grants the system camera app write access to a cache-dir capture file for the
             quick-compose "take photo" attachment flow (TakePicture contract). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode|keyboard|keyboardHidden|navigation"
            android:theme="@style/Theme.Poracode.Splash"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Canonical https://poracode.com root + /pair + /app.
                 Android treats a pathless URI as path "/"; path="/" covers root
                 pathless and root-slash. pathPrefix limits pair/app without
                 matching unrelated site paths. package id stays com.lightcodeapp.mobile. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="poracode.com" android:path="/" />
                <data android:scheme="https" android:host="poracode.com" android:pathPattern="/" />
                <data android:scheme="https" android:host="poracode.com" android:pathPrefix="/pair" />
                <data android:scheme="https" android:host="poracode.com" android:pathPrefix="/app" />
            </intent-filter>

            <!-- Custom scheme dev/simulator fallback: poracode://pair?host=https://…#token=… -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="poracode" android:host="pair" />
            </intent-filter>
        </activity>
    </application>
</manifest>
